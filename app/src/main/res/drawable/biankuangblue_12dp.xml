<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 填充颜色 -->
    <solid android:color="#FFFFFF" />
    <!-- 线的宽度，颜色灰色 -->
    <stroke
        android:width="@dimen/param_1dp"
        android:color="@color/dj_textcolorsbule" />
    <!-- 矩形的圆角半径 -->
    <corners android:radius="@dimen/param_12dp" />
</shape>